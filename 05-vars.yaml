- name: vars from another file
  hosts: local 
  connection: local
  vars_files:
    - course.yaml
  tasks:
    - name: print course details
      ansible.builtin.debug:
        msg: "course name is {{ COURSE }} and duration of the course is {{ DURATION }} "